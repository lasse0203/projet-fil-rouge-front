class UserDTO{

    constructor(id: number, pseudo : string, email: string, password: string){
        id = id
        pseudo = pseudo
        email = email
        password = password
    }
}

export default UserDTO